name: First GitHub Action
on: workflow_dispatch
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: say hello
        run: echo "test"
      
      - name: say ahmed anis
        run: |
          echo "ahmed"
          echo "anis"
